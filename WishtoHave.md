# WishtoHave — ฟังก์ชันที่อยากได้ (ยังไม่อยู่ใน roadmap)

> **ไฟล์นี้เจ้าของโปรเจกต์เขียน** — คิดอะไรออกระหว่างใช้งาน/พัฒนา จดทิ้งไว้ตรงนี้ก่อน
> ไม่ต้องคิดว่าทำได้ไหม ทำตอนไหน แพงไหม — **จดดิบ ๆ ก็พอ Claude จะประเมินให้ทีหลัง**

## วิธีใช้

**คุณทำ:** พิมพ์บรรทัดใหม่ใต้ `📥 กล่องรับ` — บรรทัดเดียวก็พอ ไม่ต้องจัดรูปแบบ

**บอก Claude ว่า:** `"อ่าน WishtoHave ให้หน่อย"` → Claude จะประเมินทีละข้อ แล้วย้ายลงไปที่ `✅ ประเมินแล้ว` พร้อมคำตอบ 5 อย่าง:

| หัวข้อ | ตอบอะไร |
|---|---|
| **ควรทำ step ไหน** | เลขที่ตรงกับ roadmap หรือ "step ใหม่" ถ้าไม่มีที่ลง |
| **กระทบโครงสร้างไหม** | แตะ schema / adapter / RLS หรือแค่ UI ชั้นนอก |
| **ต้อง migration ไหม** | ถ้าตารางมีข้อมูลจริงแล้วต้องย้ายข้อมูลด้วยหรือเปล่า |
| **⭐ ราคาถ้าเลื่อน** | ทำตอนนี้ vs ทำทีหลัง ต่างกันแค่ไหน — **ข้อสำคัญที่สุด** |
| **ติดอะไรก่อน** | ต้องรอ step ไหนเสร็จก่อนถึงทำได้ |

**ทำไม "ราคาถ้าเลื่อน" ถึงสำคัญที่สุด:** ของบางอย่างถ้าเติมตอนตารางยังว่างคือฟรี แต่ถ้ารอจนทีมกรอกข้อมูลจริงไปแล้ว
กลายเป็นงาน migration + เสี่ยงข้อมูลเพี้ยน — ต้นทุนต่างกันหลายเท่าทั้งที่เป็นฟีเจอร์เดียวกัน
(เคสจริงในโปรเจกต์นี้: `is_active`/`archived_at` ใส่ไว้ตั้งแต่ step 1.1 → step 2.5 Archive เลยไม่ต้อง migration เลย)

---

## 📥 กล่องรับ — ยังไม่ได้ประเมิน

<!-- ⬇️ เขียนต่อท้ายบรรทัดนี้ได้เลย บรรทัดละ 1 ข้อ ⬇️ -->

- ✅ **เลือก layer การแสดงผลของ dashboard ตามตำแหน่ง** _(จด 22 ก.ค. 2569 · ทำแล้ว step 3.10 · 23 ก.ค.)_
  → MD/Director/นันทวัน = เห็นทั้งองค์กร · IMP1/IMP2 = เฉพาะทีมย่อยตัวเอง · เป้าเลือกดูรายทีม/กลุ่มได้ที่ dashboard
  - **MD / admin / director** → เห็นทุกส่วน + **แก้ไขได้ในหน้าเลย**
  - **Manager กลุ่ม IMP · TA Sales** → เห็นแค่โครงการในกลุ่มของตัวเอง
  - **เปลี่ยนการกำหนดสิทธิ์ได้ภายหลัง** (ไม่ใช่ fix ตายในโค้ด)
  - ❓ ต้องถามก่อนประเมิน: "TA Sales" คือทีมไหน — ในระบบตอนนี้มี GOV.1 / GOV.3 / GOV.4 / TE-IMP / System Project
    (ถ้าเป็นทีมใหม่ ต้องเพิ่มใน `seed.sql` ด้วย)
  - 📌 ทับกับ step 2.4 อยู่มาก (role `manager` + ตาราง `team_access`) — ดูหมายเหตุใต้ตาราง




> **หมายเหตุก่อนประเมินเต็ม** (จดไว้กันลืม ยังไม่ใช่ผลประเมิน)
> ข้อนี้ **ส่วนใหญ่มีในแผน 2.4 อยู่แล้ว** — `role` บอกว่าทำอะไรได้ · `team_access` บอกว่าดูทีมไหนได้
> และแก้สิทธิ์ทีหลังได้จากหน้า Admin ตรงกับที่ขอ
> **ส่วนที่ยังไม่มีในแผน** มี 2 อย่าง คือ (1) ชั้น `director`/MD ที่สูงกว่า manager
> (2) "เลือก layer การแสดงผล" ถ้าหมายถึงสลับมุมมองเองได้ (เช่น MD กดดูแบบที่ manager เห็น)
> → ตอนประเมินต้องแยกให้ชัดว่าอยากได้แค่ **สิทธิ์ต่างกัน** หรืออยาก **สลับมุมมองได้ด้วย** เพราะราคาต่างกันมาก

---

## ✅ ประเมินแล้ว

<!-- Claude ย้ายข้อจากกล่องรับมาไว้ตรงนี้ พร้อมผลประเมิน -->

### ปุ่มลบบันทึกติดตาม
_จดเมื่อ: step 1.4c (22 ก.ค. 2569) · ประเมินเมื่อ: 22 ก.ค. 2569_

ตอนนี้บันทึกติดตามแก้ไขได้แต่ลบไม่ได้ — อยากลบบันทึกที่จดผิดทิ้ง

| หัวข้อ | ผลประเมิน |
|---|---|
| ควรทำ step ไหน | **2.6** (พร้อมหัวหน้าเซ็นรับทราบ) |
| กระทบโครงสร้างไหม | ไม่กระทบ schema — เพิ่ม policy `follow_delete` + ปุ่มใน UI |
| ต้อง migration ไหม | ไม่ต้อง |
| ⭐ ราคาถ้าเลื่อน | **ถูกลงถ้ารอ** — ทำตอนนี้ต้องกลับมาแก้ซ้ำตอน 2.6 อยู่ดี |
| ติดอะไรก่อน | step 2.4 (ต้องมี role `manager` ก่อน) |

**เหตุผลที่รอ:** ระบบนี้ทำแค่ backup ไม่มี rollback รายแถว — ลบแล้วกู้ไม่ได้
พอถึง 2.6 จะมี `signoffs` มาล็อกว่า **บันทึกที่หัวหน้าเซ็นรับทราบไปแล้วห้ามลบ**
ถ้าทำปุ่มลบตอนนี้ = เปิดช่องให้ลบหลักฐานที่หัวหน้าตรวจไปแล้ว แล้วต้องกลับมาปิดช่องทีหลัง

---

## ❌ ตัดสินใจแล้วว่าไม่ทำ

<!-- ของที่เคยคิดแล้วตัดออก — เก็บไว้กันเสนอซ้ำ/ถกซ้ำ -->

### rollback ย้อนข้อมูลรายแถว (`record_history` + trigger เก็บ old_data)
_ตัดออกเมื่อ: 21 ก.ค. 2569 (สเปค v3) · ผู้ตัดสินใจ: เจ้าของโปรเจกต์_

**ทำแทนด้วย:** Supabase backup อัตโนมัติ + ปุ่ม Export (3.6) + `import-json.html` (1.6) เป็นทางกู้คืน
\+ soft delete (`is_active=false`) สำหรับเคส "กดลบผิดตอนบ่าย" ซึ่ง backup วันละครั้งกู้ไม่ได้

🚫 **ห้ามเสนอใหม่** — บันทึกไว้ใน `CLAUDE.md` หัวข้อ "Backup & กู้คืน" แล้ว

---

## หมายเหตุ

- ของที่**อยู่ใน roadmap อยู่แล้ว** ไม่ต้องเขียนที่นี่ — ดู 23 step ใน `CLAUDE.md`
- ของที่เป็น**บั๊ก** ไม่ใช่ wish — บอก Claude ตรง ๆ ได้เลย ไม่ต้องผ่านไฟล์นี้
- ไฟล์นี้อยู่ใน repo **public** → เขียนได้เฉพาะไอเดียฟีเจอร์ **ห้ามใส่ชื่อลูกค้าจริง/เบอร์/อีเมล**








USER WISHLIST
- ~~admin มี dashboard ปุ่ม filter ดูได้ทุกทีม ทั้งรวมและแยก จาก book 3 สี, pending project~~ ✅ **ทำแล้ว** (24 ก.ค. 2569) — ตัวกรองทีมบนสุดของหน้าภาพรวม: KPI/กราฟ/funnel/top3/เลยกำหนด + การ์ดลูกค้า Book 3 สี กรองตามทีม (มุมมอง "รวม") · ตารางเป้ารายทีม = มุมมอง "แยก"
- หน้า page แต่ละโครงการ pending มีปุ่ม shortcut เลื่อนลงไปกรอกด้านล่างสุด ได้ทันที
- ~~เลือก export ให้รูปแบบ เหมือน ไฟล์ แบบ ฟอร์ม book 3 สี ,pending ต้นฉบับ~~ ✅ **ทำแล้ว step 3.9** (23 ก.ค. 2569)
- การลิงค์เชื่อมโยง book 3 สี กับ pending project ลูกค้า ลิงค์ไปที่งานได้
- เพิ่มรูปลูกค้า ในฐานข้อมูล 
- มีปุ่ม ปรับการแสดงผลให้เหมือน book 3 สี , pending project จริงๆได้ หรือ จะใช้แบบเดิมปัจจุบันก็ยังได้
- ธีม สว่าง / มืด  📋 _อยู่ในแผน step 3.7 แล้ว_
- ตั้ง username password ได้ใน app
- ~~สร้างเป็น app บน iphone, ipad , android~~ ✅ **ทำแล้ว step 3.3** (PWA ติดตั้งได้ทั้ง 3 เครื่อง)
- วางไว้บน web จริง ที่มองไม่เห็น source code
 - มีที่สำรอง supabase 